{
	"dependencies": {
		"styles": "github:flamebuckler/styles"
	},
	"devDependencies": {
		"sass": "^1.73.0",
		"wrangler": "^4.67.1"
	},
	"scripts": {
		"css:build": "sass --load-path=node_modules scss/main.scss public/css/main.css --no-source-map",
		"css:watch": "sass --load-path=node_modules --watch scss:public/css --no-source-map"
	}
}
